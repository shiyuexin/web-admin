const state = {
    name: null, // 用户名
};

const mutations = {
    SET_NAME: (state, name) => {
        state.name = name;
    }
};

const actions = {

};

export default {
    state,
    mutations,
    actions
}